# Path to the SQLite database
DB_PATH = "../results_processed/experiment_results.db"

# Directory for saving visualization results
OUTPUT_DIR = "visualization_results"

# Directory for raw results (if needed elsewhere)
RAW_RESULTS_DIR = "../results_raw"

# Default settings for scripts
INCLUDE_PRACTICE = False  # Include practice data in processing
INCLUDE_ONLY_DIGITS = None  # Specify digits to include, e.g., [0, 1, 2]. None means include all.
